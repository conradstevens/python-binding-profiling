[project]
name = "numba-package"
version = "0.1.0"
description = "This a toy package for profiling function"
requires-python = ">=3.12,<3.13"
dependencies = [
    "numpy>=1.24.4",
    "numba>=0.60.0",
]

[tool.hatch.build.targets.wheel]
include = ["src/numba_package"]

[tool.uv]
package = true