[project]
name = "python-package"
version = "0.1.0"
description = "This a toy package for profiling functions"
requires-python = ">=3.10,<3.13"
dependencies = [
    "numpy>=1.24.4",
]

[tool.hatch.build.targets.wheel]
include = ["src/python_package"]

[tool.uv]
package = true