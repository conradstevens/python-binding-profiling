[project]
name = "pure-python-package"
version = "0.1.0"
description = "This a toy package for profiling function"
requires-python = ">=3.12,<3.13"
dependencies = [
    "numpy>=1.24.4",
]

[tool.hatch.build.targets.wheel]
include = ["src/pure_python"]

[tool.uv]
package = true